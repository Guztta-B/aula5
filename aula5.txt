#include <iostream>

using namespace std;

int main()
{
 int a;
 int b;

 cout << "Insira o primeiro valor numero: ";
 cin >> a;
 cout << "Insira o segundo numero: " ;
 cin >> b;

 int soma = a + b;
 int subtracao = a - b;
 int multiplicacao = a * b;
 int divisao = a / b;
 int resto = a % b;

 cout << "soma: " << soma << endl;
 cout << "subtracao: " << subtracao << endl;
 cout << "multiplicacao: " << multiplicacao << endl;
 cout << "divisao: " << divisao << endl;
 cout << "resto: " << resto << endl;


 if(resto>0) {
    float divp =(float) a/b;
    cout << divp;
 }

return 0;
}
